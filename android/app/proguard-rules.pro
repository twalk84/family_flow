# Flutter / Firebase baseline
# (Most Flutter apps can keep this file minimal unless you enable shrinking)

# Keep Kotlin metadata (rarely needed, but safe)
-keep class kotlin.Metadata { *; }

# If you later enable shrinking and see runtime reflection issues,
# you’ll add specific keep rules here based on R8 warnings.
